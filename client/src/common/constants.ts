export const API_URL = process.env.REACT_APP_API_URL || "http://localhost:8080";
export const genericWordsWithSpacesRegex = /^(?:\s+[A-Za-z0-9]+)+\s+$/;
export const REGISTERED_PATHS = {
  HOME: "/",
};
